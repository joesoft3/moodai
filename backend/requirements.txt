fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy[asyncio]>=2.0
asyncpg>=0.29
pydantic>=2.7
pydantic-settings>=2.4
email-validator>=2.0
python-jose[cryptography]>=3.3
cryptography>=42
passlib>=1.7
python-multipart>=0.0.9
openai>=1.50
qdrant-client>=1.11
# fastembed intentionally NOT here: it pulls onnxruntime (~200 MB unpacked) which
# exceeds serverless function bundles (Vercel 250 MB). Container/dev hosts get it
# via requirements-full.txt; slim hosts automatically use the embeddings API
# fallback (see app/services/memory.py).
redis>=5.0
httpx>=0.27
pypdf>=4.2
python-docx>=1.1
openpyxl>=3.1
pillow>=10.4
stripe>=10.0
dnspython>=2.6
alembic>=1.13
prometheus-client>=0.20
opentelemetry-sdk>=1.27
opentelemetry-exporter-otlp-proto-http>=1.27
opentelemetry-instrumentation-fastapi>=0.48b0
opentelemetry-instrumentation-httpx>=0.48b0
aiosqlite>=0.20
imageio-ffmpeg>=0.5.1   # static ffmpeg fallback for apt-less hosts (Vercel/Lambda); unused when ffmpeg is on PATH
boto3>=1.34             # Cloudflare R2 storage backend (S3-compatible); lazy-imported, only used when R2_* is set
