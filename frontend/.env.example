# Mood AI web — the only env the frontend needs at BUILD time (Next.js inlines NEXT_PUBLIC_*).
# Local dev default is http://localhost:8000/api/v1 (no file needed).
# Netlify/Vercel: set this in the dashboard → Site settings → Environment variables.
NEXT_PUBLIC_API_URL=https://api.your-domain.com/api/v1
